import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'chat-ui-ionic6',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
